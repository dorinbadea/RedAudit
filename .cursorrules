# Repository Workflow Pointer

This file is an IDE pointer. Canonical workflow is defined in `AGENTS.md` (root).

Follow the **Multi-IDE Pointer Contract** in `AGENTS.md`.
If anything conflicts, `AGENTS.md` is canonical.
