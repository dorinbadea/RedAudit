# Repository Workflow Pointer

This repo's workflow is defined in `AGENTS.md` (root). Always follow it.

- Read `AGENTS.md` at the start of every task.
- If anything conflicts, `AGENTS.md` is canonical.
